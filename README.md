# 插件抽屉（plugin-hub）

Hana 插件抽屉。管理顶栏插件置顶/收纳、文件夹分类、主题切换。

## 安装

1. 下载 [最新 Release](https://github.com/hayou2002/plugin-hub/releases/latest) 的 zip
2. 打开 Hana → 插件管理
3. 拖入 zip 安装，重启 Hana

## 用法

### 浏览模式（默认）
- 安装后顶栏出现「抽屉」标签
- 隐藏的插件按文件夹分组展示，点击即可打开
- 右上角 🎨 按钮切换主题配色

### 管理模式
- 点击右上角 ⚙ 管理进入
- 通过开关控制每个插件的显示/隐藏
- 创建文件夹对插件进行分类
- 一键「全部收进」或「全部置顶」

### 隐藏顶栏 ▼
- 进入管理模式，点击「隐藏▼」按钮
- 自动重装增强补丁，重启 Hana 后顶栏 ▼ 溢出按钮永久消失
- 新用户安装插件后首次收纳即自动触发补丁安装

### 主题
- 6 个预设主题：极光、落日、海洋、樱花、森林、午夜
- 选择自动保存，下次打开自动应用

## 版本历史

### v1.5.1（当前）
- 修复：创建文件夹功能（+ 按钮事件冒泡被拦截导致无法创建）
- 修复：文件夹创建后 UI 不刷新的竞态问题
- 新增：管理页「隐藏▼」按钮 — 一键重装补丁，重启后顶栏 ▼ 溢出按钮永久消失
- 补丁引擎升级：支持 force 模式直写 artifacts/renderer 解压版（兼容多版本 Hana）

### v1.5.0
- 全新 UI：浏览模式 + 管理模式分离
- 主题选择器：6 个预设配色，支持自定义切换
- 液态玻璃风格界面，多彩渐变图标
- 移除增强补丁功能（回归纯插件方案）

### v1.4.0
- 秒开架构：CSS/JS 外部化 + 进程级缓存
- 增强补丁：原生下拉悬浮展开

### v1.3.0
- 初始版本

## 文件结构

```
plugin-hub/
  manifest.json          ← 插件配置
  index.js               ← 入口
  routes/
    hub.js               ← 路由 + HTML 模板
    patcher.js           ← 补丁引擎（保留备用）
  assets/
    hub.css              ← 样式（液态玻璃 + 主题变量）
    hub.js               ← 前端逻辑（浏览/管理/主题）
  tools/
    install-patch.js     ← 补丁安装工具（保留备用）
  README.md
```

## 兼容性

- `minAppVersion`: 0.82.0
- 支持 Windows / macOS / Linux
- 自动适配明暗主题
